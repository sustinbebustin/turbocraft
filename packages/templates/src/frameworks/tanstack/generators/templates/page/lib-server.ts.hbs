import { createServerFn } from "@tanstack/react-start";

export const get{{pascalCase name}} = createServerFn({ method: "GET" }).handler(
  async () => {
    // TODO: implement server-only data access for {{name}}.
    return null;
  }
);
