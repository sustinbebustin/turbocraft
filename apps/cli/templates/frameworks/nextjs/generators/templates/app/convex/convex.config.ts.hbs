import { defineApp } from "convex/server";
{{#if withBetterAuth}}
import betterAuth from "@convex-dev/better-auth/convex.config";
{{/if}}

const app = defineApp();
{{#if withBetterAuth}}
app.use(betterAuth);
{{/if}}

export default app;
